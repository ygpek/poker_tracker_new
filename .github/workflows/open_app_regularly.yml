name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 7,15,22 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -I --max-time 30 https://kartydengidvatuza.streamlit.app/ || true
